// there are three methods of decalring array in javascript

// first. simple using array literal
// second. using instance of array
// third. using constructor

let arr = [1,2,3,4,5,6];
for (let i in arr){
  console.log("arr["+i+"] : ",arr[i]);
}