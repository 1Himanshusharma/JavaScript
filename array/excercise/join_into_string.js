let arr = ["Red","Green","White","Black"];

// using the toString method to convert the array to string
console.log(arr.toString());


//  using the default join method to concate array elments into a string sep by coma.
console.log(arr.join());

//using the join method with a custom sep ('+')

console.log(arr.join('+'));