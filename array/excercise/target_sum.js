function twoSum(nums, target_num){
  // initialize an empty array 'map' to store the diff
  let map = [];
  let index_num = [];

  // iterate through  each element in the nums
  for (let i : nums){
    if (map[nums[i]] != null ){
      // if found, store the indices of the two numbers and break out 
      let index = map[nums[i]];
      index_num[0] = index;
      index_num[1] = i'
      
    }
  }
}